\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{amsmath}

\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  urlcolor=blue,
  pdftitle={DeepFake Defender Repository Technical Handbook},
  pdfauthor={Project Technical Study}
}

\setlist[itemize]{leftmargin=1.3em}
\setlist[enumerate]{leftmargin=1.3em}
\newcommand{\fpath}[1]{\path{#1}}

\title{DeepFake Defender (AI Media Detection Tool)\\Repository Technical Handbook\\Architecture, Runtime Flow, and Operations}
\author{Technical Study Document}
\date{\today}

\begin{document}
\maketitle
\tableofcontents
\newpage

\section{Document Purpose}
This handbook is a deep technical reference for the current repository state.
It is intended for:
\begin{enumerate}
  \item developers onboarding to the codebase,
  \item operators deploying and debugging runtime issues,
  \item reviewers who need an accurate image and video pipeline map.
\end{enumerate}

This document reflects the current implementation in:
\begin{enumerate}
  \item backend API orchestration,
  \item model fusion and threshold logic,
  \item forensic module behavior,
  \item Windows-first runtime scripts,
  \item frontend scan/report flow.
\end{enumerate}

\section{Repository Snapshot and Major Components}
\subsection{Top-Level Layout}
\begin{longtable}{p{0.40\textwidth} p{0.54\textwidth}}
\toprule
\textbf{Path} & \textbf{Purpose} \\
\midrule
\endhead
\fpath{README.md} & User-facing setup and operations guide. \\
\fpath{repository_study.tex} & Technical handbook (this document). \\
\fpath{deployment_document.md} & Deployment notes for hosted scenarios. \\
\fpath{requirements.txt} & Python dependencies for backend and runtime modules. \\
\fpath{backend/} & Flask app, model fusion, forensic modules, C2PA/SynthID services. \\
\fpath{frontend/} & React + Vite application for image/video scan and reports. \\
\fpath{scripts/windows/} & Windows startup and health-check scripts. \\
\fpath{model_output/} & Runtime model artifacts (ResNet and SynthID assets). \\
\fpath{ai_detector_meta_learner.joblib} & Optional meta-learner for image score fusion. \\
\fpath{polynomial_transformer.joblib} & Feature transform used by meta-learner. \\
\bottomrule
\end{longtable}

\subsection{Backend Core Files}
\begin{longtable}{p{0.40\textwidth} p{0.54\textwidth}}
\toprule
\textbf{Path} & \textbf{Purpose} \\
\midrule
\endhead
\fpath{backend/app.py} & Main Flask API, layer orchestration, async runtime init, health endpoint. \\
\fpath{backend/combine_model.py} & ResNet + ViT + meta learner fusion, forensic-aware thresholding, final labeling. \\
\fpath{backend/custom_forensics.py} & Frequency, anatomy, and entropy forensic analyzers and calibration logic. \\
\fpath{backend/forensic.py} & Human-readable summary and forensic report generation. \\
\fpath{backend/video_detect_standalone.py} & Dedicated video model path plus frame-based fallback path. \\
\fpath{backend/src/c2pa_checker.py} & C2PA runtime availability and manifest parsing. \\
\fpath{backend/src/synthid/service.py} & SynthID watermark service with calibrated decision logic. \\
\fpath{backend/serve.py} & Waitress production entrypoint. \\
\bottomrule
\end{longtable}

\subsection{Frontend Core Files}
\begin{longtable}{p{0.40\textwidth} p{0.54\textwidth}}
\toprule
\textbf{Path} & \textbf{Purpose} \\
\midrule
\endhead
\fpath{frontend/src/App.jsx} & Route map for home, image dashboard/report, and video dashboard/report. \\
\fpath{frontend/src/pages/Dashboard.jsx} & Image upload pipeline and live step logging. \\
\fpath{frontend/src/pages/Report.jsx} & Image verdict and forensic summary visualization. \\
\fpath{frontend/src/pages/VideoDashboard.jsx} & Video upload and analysis pipeline. \\
\fpath{frontend/src/pages/VideoReport.jsx} & Video verdict and forensic summary display. \\
\fpath{frontend/vite.config.js} & Dev server and API proxy (\texttt{/api} to backend port 7860). \\
\bottomrule
\end{longtable}

\section{Runtime Architecture}
\subsection{High-Level Layered Pipeline}
Image analysis uses a strict short-circuit decision sequence:
\begin{enumerate}
  \item Layer 1: C2PA metadata check,
  \item Layer 2: SynthID watermark check,
  \item Layer 3: AI model plus forensic calibration.
\end{enumerate}

The first decisive AI-positive layer short-circuits lower layers.

Video analysis uses:
\begin{enumerate}
  \item dedicated checkpoint model path when checkpoints are present,
  \item fallback frame scoring path when dedicated checkpoints are missing or fail.
\end{enumerate}

\subsection{Asynchronous Initialization in \fpath{backend/app.py}}
At app startup:
\begin{enumerate}
  \item Flask server binds immediately,
  \item a background daemon thread starts \texttt{initialize\_all()},
  \item startup attempts Hugging Face downloads for missing assets,
  \item predictor and SynthID service are loaded,
  \item runtime ready flag is set when core objects are initialized.
\end{enumerate}

This design allows early health checks but may return temporary 503 for \texttt{/api/analyze} before model readiness.

\section{Backend Route Contracts}
\subsection{Route List}
\begin{longtable}{p{0.22\textwidth} p{0.14\textwidth} p{0.58\textwidth}}
\toprule
\textbf{Route} & \textbf{Method} & \textbf{Purpose} \\
\midrule
\endhead
\fpath{/} & GET & Template UI landing page. \\
\fpath{/dashboard} & GET & Template image scan page. \\
\fpath{/video\_dashboard} & GET & Template video scan page. \\
\fpath{/report} & GET & Template image report page. \\
\fpath{/video\_report} & GET & Template video report page. \\
\fpath{/api/health} & GET & Runtime status, Python executable/version, model and C2PA status. \\
\fpath{/api/analyze} & POST & Image analysis cascade. \\
\fpath{/api/analyze\_video} & POST & Video analysis (dedicated or fallback path). \\
\fpath{/api/forensic-report} & POST & Forensic markdown-style report generation from analysis JSON. \\
\bottomrule
\end{longtable}

\subsection{Health Endpoint Semantics}
\fpath{/api/health} returns:
\begin{enumerate}
  \item Python runtime metadata,
  \item global model initialization state,
  \item per-model load/error state,
  \item C2PA availability and API capabilities.
\end{enumerate}

Critical operational field: \texttt{python\_executable}.
If this points to system Python instead of the project virtual environment, behavior and installed package availability can diverge from expectation.

\section{Image Analysis Runtime Flow}
\subsection{Request Validation}
\fpath{/api/analyze} accepts multipart file uploads with:
\begin{enumerate}
  \item extensions in \{png, jpg, jpeg, webp\},
  \item max request size 16 MB.
\end{enumerate}

If predictor is not ready:
\begin{enumerate}
  \item returns 503 while loading, or
  \item attempts runtime recovery and returns 503 if initialization still fails.
\end{enumerate}

\subsection{Layer 1: C2PA}
\texttt{check\_c2pa(filepath)} produces status and manifest-derived flags.

Decision policy:
\begin{enumerate}
  \item if \texttt{c2pa\_present = true}, image is finalized as AI generated,
  \item SynthID and model layers are marked skipped.
\end{enumerate}

Verdict text differentiates:
\begin{enumerate}
  \item explicit AI assertion case,
  \item metadata-presence policy case.
\end{enumerate}

\subsection{Layer 2: SynthID}
Executed only when C2PA metadata is not present.

If SynthID returns calibrated watermark positive:
\begin{enumerate}
  \item final verdict is AI generated,
  \item model layer is skipped.
\end{enumerate}

Otherwise pipeline continues to Layer 3.

\subsection{Layer 3: Model + Forensics}
\texttt{AIEnsemblePredictor.predict(..., return\_details=True)} provides:
\begin{enumerate}
  \item label (AI Image / Real Image),
  \item confidence and AI probability,
  \item threshold and score internals,
  \item forensic module outputs,
  \item explainability artifacts.
\end{enumerate}

\section{Model Fusion in \fpath{backend/combine_model.py}}
\subsection{Component Models}
\begin{enumerate}
  \item ResNet50 is required and loaded from \fpath{model\_output/resnet50\_finetuned\_benchmark.pth}.
  \item ViT is optional and loaded from Hugging Face model \texttt{dima806/ai\_vs\_real\_image\_detection} unless \texttt{SKIP\_VIT=1}.
  \item Meta learner is optional and requires both \fpath{ai\_detector\_meta\_learner.joblib} and \fpath{polynomial\_transformer.joblib}.
\end{enumerate}

\subsection{TTA Behavior}
If \texttt{ENABLE\_TTA=1}, the predictor evaluates:
\begin{enumerate}
  \item original image,
  \item mirrored image.
\end{enumerate}
Scores are averaged across views.

\subsection{Legacy and Meta Fusion}
Given ResNet score $p_r$ and ViT score $p_v$:
\[
p_{blend} = \frac{p_r + p_v}{2}
\]
(if ViT unavailable, $p_{blend}=p_r$).

If meta learner is available, $p_{meta}$ is computed from polynomial features of $(p_r, p_v)$.
Depending on version warning and drift, final legacy score uses direct or blended meta usage.

\subsection{Custom Forensic Blend}
Custom forensic module produces calibrated score $p_c$ and forensics-only score $p_f$.
If no supporting modules exist:
\[
p_{final,pre} = 0.70\,p_{legacy} + 0.30\,p_c
\]
Else stronger model-forensic behavior is used based on support level.

\subsection{Disagreement Guards}
Two guards refine score behavior:
\begin{enumerate}
  \item ViT disagreement guard: prevents false real outcomes in high-confidence AI disagreement contexts with forensic corroboration.
  \item Real disagreement guard: reduces false AI outcomes when forensic support is weak.
\end{enumerate}

Score-consensus refinement then adjusts selected disagreement cases.

\section{Dynamic Threshold Resolution}
\subsection{Base Threshold Bands}
Threshold is context-dependent (supporting modules, negative modules, forensics-only score, ViT availability).

Representative values with ViT available:
\begin{enumerate}
  \item strong support: 0.54 to 0.62,
  \item weak/no support: 0.68 to 0.76,
  \item very strong model scenarios may lower threshold when corroboration exists.
\end{enumerate}

\subsection{Low-Corroboration Safety Guard}
A specific guard raises threshold to 0.86 when all are true:
\begin{enumerate}
  \item no supporting forensic modules,
  \item anatomy module unavailable,
  \item at least one negative module,
  \item forensics-only score is weak,
  \item frequency and entropy support are weak.
\end{enumerate}

This avoids model-only false AI outcomes when corroborating forensic evidence is insufficient.

\subsection{Decision and Confidence}
Final decision:
\[
\text{AI if } p_{final} \ge t, \quad \text{else Real}
\]
where $t$ is the resolved threshold.

Confidence is computed from relative margin:
\[
\text{conf} = 0.5 + 0.5\,\mathrm{clip}(m,0,1)
\]
with
\[
m = \begin{cases}
\dfrac{p_{final}-t}{1-t}, & \text{AI decision} \\
\dfrac{t-p_{final}}{t}, & \text{Real decision}
\end{cases}
\]

\section{Forensic Modules in \fpath{backend/custom_forensics.py}}
\subsection{Module Set}
\begin{enumerate}
  \item Frequency artifacts (FFT checkerboard/upsampling patterns),
  \item Anatomy consistency (MediaPipe pose and hand geometry),
  \item Semantic entropy consistency.
\end{enumerate}

\subsection{Weights}
Custom weighted score uses:
\begin{enumerate}
  \item ResNet: 0.50,
  \item Frequency: 0.20,
  \item Anatomy: 0.20,
  \item Entropy: 0.10.
\end{enumerate}

\subsection{Support and Negative Classifications}
\begin{enumerate}
  \item support threshold: 0.60,
  \item negative threshold: 0.15.
\end{enumerate}

\subsection{Dependency-Driven Availability}
Anatomy can be unavailable when:
\begin{enumerate}
  \item MediaPipe legacy API is incomplete, and
  \item Tasks API model files (\texttt{pose\_landmarker.task}, \texttt{hand\_landmarker.task}) are missing.
\end{enumerate}

Entropy can be unavailable if \texttt{scikit-image} is not installed.

\section{C2PA Internals}
\subsection{Runtime Status}
\fpath{backend/src/c2pa_checker.py} exposes runtime status fields:
\begin{enumerate}
  \item \texttt{available},
  \item \texttt{import\_error},
  \item \texttt{has\_reader},
  \item \texttt{has\_c2pa\_error}.
\end{enumerate}

\subsection{Manifest Parsing}
When reader API is available:
\begin{enumerate}
  \item parse manifest store,
  \item inspect active manifest,
  \item inspect action assertions for AI signal,
  \item return normalized C2PA result dictionary.
\end{enumerate}

\section{SynthID Service Internals}
\subsection{Calibration Inputs}
\fpath{backend/src/synthid/service.py} evaluates:
\begin{enumerate}
  \item raw confidence,
  \item phase match,
  \item correlation margin,
  \item structure ratio,
  \item carrier match ratio,
  \item multi-scale consistency,
  \item composed signal score.
\end{enumerate}

\subsection{Output Bands}
Service emits signal bands:
\begin{enumerate}
  \item \texttt{strong}: calibrated positive,
  \item \texttt{weak}: watermark-like but below calibrated threshold,
  \item \texttt{negative}: no watermark evidence.
\end{enumerate}

\section{Video Analysis Runtime Flow}
\subsection{Dedicated Path}
In \fpath{backend/video_detect_standalone.py}, dedicated path requires both:
\begin{enumerate}
  \item \fpath{backend/checkpoints/efficientnet.onnx},
  \item \fpath{backend/checkpoints/model.pth}.
\end{enumerate}

It samples 3 frames and uses threshold 0.60.

\subsection{Fallback Path}
Fallback path samples 6 frames and scores each frame with the image predictor.

Let frame probabilities be $\{p_i\}$:
\[
p_{mean}=\frac{1}{N}\sum_i p_i, \qquad p_{peak}=\max_i p_i
\]
\[
p_{video}=\mathrm{clip}(0.7\,p_{mean}+0.3\,p_{peak}, 0, 1)
\]
Decision threshold for fallback is 0.65.

\section{Frontend Runtime Flow}
\subsection{Route Graph}
\fpath{frontend/src/App.jsx} routes:
\begin{enumerate}
  \item \fpath{/} home,
  \item \fpath{/dashboard} image scan,
  \item \fpath{/report} image report,
  \item \fpath{/video-dashboard} video scan,
  \item \fpath{/video-report} video report.
\end{enumerate}

\subsection{Session Storage Hand-off}
Image flow:
\begin{enumerate}
  \item dashboard stores API response in \texttt{sessionStorage.analysisResult},
  \item report reads and clears this key.
\end{enumerate}

Video flow follows analogous pattern with \texttt{videoAnalysisResult}.

\subsection{Display Semantics}
Report screens primarily show decision confidence and labels from backend response.
Important: displayed confidence is not the same field as raw AI probability.

\section{Dependencies and Runtime Assets}
\subsection{Python Dependencies}
\fpath{requirements.txt} includes:
\begin{enumerate}
  \item Flask stack: \texttt{flask}, \texttt{werkzeug}, \texttt{waitress},
  \item ML stack: \texttt{torch}, \texttt{torchvision}, \texttt{transformers}, \texttt{numpy}, \texttt{scikit-learn}, \texttt{joblib},
  \item forensic stack: \texttt{scikit-image}, \texttt{mediapipe}, \texttt{PyWavelets}, \texttt{scipy},
  \item video stack: \texttt{opencv-python}, \texttt{onnx}, \texttt{onnx2pytorch},
  \item provenance stack: \texttt{c2pa-python}.
\end{enumerate}

\subsection{Frontend Dependencies}
\fpath{frontend/package.json} uses:
\begin{enumerate}
  \item React 18,
  \item React Router 6,
  \item Vite 5,
  \item Tailwind toolchain.
\end{enumerate}

\subsection{Asset Matrix}
\begin{longtable}{p{0.34\textwidth} p{0.20\textwidth} p{0.38\textwidth}}
\toprule
\textbf{Asset} & \textbf{Required} & \textbf{Behavior if Missing} \\
\midrule
\endhead
\fpath{model_output/resnet50_finetuned_benchmark.pth} & Yes & Predictor initialization fails. \\
\fpath{ai_detector_meta_learner.joblib} & No & Meta fusion disabled, baseline fusion used. \\
\fpath{polynomial_transformer.joblib} & No & Meta fusion disabled, baseline fusion used. \\
\fpath{model_output/synthid/robust_codebook.pkl} & No (for full 3-layer yes) & SynthID service unavailable; layer marked unavailable. \\
ViT Hugging Face weights & No & ViT disabled; ResNet + forensics path used. \\
\fpath{backend/checkpoints/efficientnet.onnx} + \fpath{backend/checkpoints/model.pth} & No & Video fallback frame path used. \\
MediaPipe task files & No & Anatomy module may be unavailable. \\
\bottomrule
\end{longtable}

\section{Environment Variables}
\begin{longtable}{p{0.35\textwidth} p{0.18\textwidth} p{0.39\textwidth}}
\toprule
\textbf{Name} & \textbf{Default} & \textbf{Scope/Purpose} \\
\midrule
\endhead
\texttt{PORT} & 7860 & Backend listen port (dev and prod entrypoints). \\
\texttt{HOST} & 0.0.0.0 & Waitress bind host. \\
\texttt{WAITRESS\_THREADS} & 8 & Waitress thread count. \\
\texttt{HF\_TOKEN} & unset & Hugging Face authenticated downloads. \\
\texttt{ENABLE\_TTA} & 1 & Enable mirrored-view scoring. \\
\texttt{RESNET\_AI\_INDEX} & 1 & ResNet AI class index. \\
\texttt{SKIP\_VIT} & 0 & Skip ViT load when set to 1. \\
\texttt{VIT\_AI\_INDEX} & 1 & ViT AI class fallback index. \\
\texttt{MEDIAPIPE\_POSE\_MODEL\_PATH} & unset & Pose task model override path. \\
\texttt{MEDIAPIPE\_HAND\_MODEL\_PATH} & unset & Hand task model override path. \\
\texttt{SYNTHID\_MIN\_CONFIDENCE} & 0.58 & SynthID calibrated threshold input. \\
\texttt{SYNTHID\_MIN\_PHASE\_MATCH} & 0.48 & SynthID calibrated threshold input. \\
\texttt{SYNTHID\_MIN\_CORRELATION\_MARGIN} & 0.0018 & SynthID calibrated threshold input. \\
\texttt{SYNTHID\_MIN\_CARRIER\_MATCH\_RATIO} & 0.30 & SynthID calibrated threshold input. \\
\texttt{SYNTHID\_MIN\_SIGNAL\_SCORE} & 0.56 & SynthID calibrated threshold input. \\
\texttt{SYNTHID\_MIN\_STRUCTURE\_RATIO} & 0.78 & SynthID calibrated threshold input. \\
\texttt{SYNTHID\_MAX\_STRUCTURE\_RATIO} & 1.85 & SynthID calibrated threshold input. \\
\texttt{SYNTHID\_MAX\_MULTI\_SCALE\_STD} & 0.18 & SynthID calibrated threshold input. \\
\bottomrule
\end{longtable}

\section{Windows Operations and Failure Modes}
\subsection{Canonical Startup Scripts}
\begin{enumerate}
  \item \fpath{scripts/windows/start-backend-dev.ps1}: starts \fpath{backend/app.py} using project \fpath{.venv} interpreter.
  \item \fpath{scripts/windows/start-backend-prod.ps1}: starts \fpath{backend/serve.py} (Waitress).
  \item \fpath{scripts/windows/start-frontend-dev.ps1}: starts Vite frontend.
  \item \fpath{scripts/windows/health-check.ps1}: queries \fpath{/api/health}.
\end{enumerate}

\subsection{Common Operational Problems}
\begin{longtable}{p{0.28\textwidth} p{0.66\textwidth}}
\toprule
\textbf{Problem} & \textbf{Resolution} \\
\midrule
\endhead
Stale process on port 7860 & Find and kill bound PID, then restart backend from script. \\
Wrong Python runtime & Verify \texttt{python\_executable} in health response points to project \fpath{.venv}. \\
Temporary 503 on analyze & Wait for async initialization, then retry when \texttt{runtime\_initialized=true}. \\
ViT startup delays & Use \texttt{SKIP\_VIT=1} for environments with constrained network. \\
C2PA unavailable & Reinstall \texttt{c2pa-python} in active virtual environment and re-check health. \\
Anatomy unavailable & Install or repair MediaPipe, or provide task files and env path overrides. \\
Frontend appears stale & Trigger a fresh scan; report pages read from sessionStorage snapshot. \\
\bottomrule
\end{longtable}

\section{Verification Checklist}
\subsection{Setup Validation}
\begin{enumerate}
  \item Backend starts without import failures.
  \item \fpath{/api/health} returns success and expected Python path.
  \item Model readiness fields indicate loaded predictor and SynthID service.
  \item Frontend can call backend through Vite proxy.
\end{enumerate}

\subsection{Image Path Validation}
\begin{enumerate}
  \item Upload image without C2PA and without SynthID watermark.
  \item Confirm Layer 3 executes and returns model score details.
  \item Confirm forensic summary reflects available modules only.
\end{enumerate}

\subsection{Video Path Validation}
\begin{enumerate}
  \item With checkpoints missing, confirm fallback source and frame explainability fields.
  \item With checkpoints available, confirm dedicated path and threshold behavior.
\end{enumerate}

\section{Current Notable Updates Reflected in This Version}
\begin{enumerate}
  \item Health-path diagnostics are emphasized due frequent runtime mismatch on Windows.
  \item C2PA-first short-circuit policy and SynthID short-circuit policy are explicitly documented.
  \item Score-consensus and disagreement guard logic in model fusion is documented.
  \item Low-corroboration threshold guard (0.86) is documented as current behavior.
  \item Video fallback aggregation formula and thresholds are documented.
  \item Frontend sessionStorage hand-off behavior is documented.
\end{enumerate}

\section{Conclusion}
The repository implements a layered, policy-driven detection system with practical operational safeguards.
Accurate interpretation requires understanding:
\begin{enumerate}
  \item short-circuit layer precedence,
  \item model probability versus decision confidence,
  \item dynamic threshold guards under weak forensic corroboration,
  \item runtime environment correctness (especially on Windows).
\end{enumerate}

Use this document together with \fpath{README.md}:
\begin{enumerate}
  \item README for setup and day-to-day operation,
  \item this handbook for architecture and deep runtime study.
\end{enumerate}

\end{document}
